# Copy this file to backend/.env (never commit backend/.env).
# This file is read by python-dotenv inside database.py.
# Update the password to match POSTGRES_PASSWORD in the root .env file.

DATABASE_URL=postgresql://admin:changeme_strong_password@localhost:5432/asset_compliance
